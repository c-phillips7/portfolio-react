import React from 'react';

//future development, auto pull from github?
//Image with small title
    // on click anywhere on image takes user to github page
    // on hover show options of github, or deployed page

export default function Work() {
  return (
    <div>
      <h1>Github Projects with photo displaying them</h1>
      <li>
        Github projects of note
      </li>
    </div>
  );
}
